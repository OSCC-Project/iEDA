#===========================================================
##   read sdc
#===========================================================
if {[info exists ::env(SDC_FILE)] && $SDC_FILE ne ""} {
    db_init -sdc_path $SDC_FILE
}
